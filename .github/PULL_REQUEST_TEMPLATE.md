## What changed and why

## How to verify

- [ ] `pnpm lint`, `pnpm typecheck`, `pnpm test`, and `pnpm build` all pass locally.
- [ ] If this touches a page's UI, I checked it in a browser at desktop and mobile widths.
- [ ] If this adds or edits benefit data, I linked a real source and left `verificationStatus` honest (not marked VERIFIED unless I actually confirmed it today).

## Screenshots (if this changes UI)
